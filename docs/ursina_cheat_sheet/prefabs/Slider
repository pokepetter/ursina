Slider(min=0, max=1, default=0, **kwargs)

label # Text
bg
knob
value
dynamic = True # call on_value_changed while dragging / call only when dropped

on_value_changed() # gets called if implemented


example:
  e = Entity(model='cube', color=color.white)
  my_slider = Slider()

  def update():
    e.color = color.color(0, 0, my_slider.value)
