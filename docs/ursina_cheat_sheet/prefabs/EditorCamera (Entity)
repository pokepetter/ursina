# basic mouse controlled camera useful for editor
EditorCamera()
camera_pivot
rotation_speed = 100
pan_speed = (4, 4)
move_speed = .1
zoom_speed = 1

# '+' to increase fov
# '-' to decrease fov
# 'p' to toggle orthographic
