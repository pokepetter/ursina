example: entity.collider = 'box'

# to automatically fit mesh bounds
'box'
'sphere'
#todo 'mesh'
#todo 'capsule'
#todo 'plane'

BoxCollider(entity, center, (width=1, height=1, depth=1))
SphereCollider(entity, center, radius=.5)
